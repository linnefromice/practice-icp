type Snapshot = record { value : Transfer; timestamp : nat64 };
type Transfer = record { to : text; from : text; amount : nat64 };
service : {
  greet : (text) -> (text) query;
  vec_insert : (Transfer) -> ();
  vec_select_all : () -> (vec Snapshot) query;
}