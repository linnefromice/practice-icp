# ws_shell_1

```bash
cargo make did
dfx stop && dfx start --clean --background && dfx deploy

dfx canister call backend get --query
dfx canister call backend inc
dfx canister call backend get --query

dfx canister call backend get --query
dfx canister call backend set '(50)'
dfx canister call backend set '(-50)'
```
